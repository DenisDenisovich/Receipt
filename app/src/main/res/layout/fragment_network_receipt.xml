<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@color/background">
    <include
        layout="@layout/fragment_receipt"
        android:id="@+id/view_receipt"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginBottom="16dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/container_receipt_save" />


    <FrameLayout
        android:id="@+id/container_receipt_save"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">
        <Button
            android:id="@+id/btn_receipt_save"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:background="@drawable/btn_receipt_save"
            android:elevation="8dp"
            android:paddingLeft="16dp"
            android:paddingRight="16dp"
            android:text="Сохранить"
            android:textColor="#FFFFFF" />

        <ProgressBar
            android:id="@+id/pb_receipt_save"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:elevation="8dp"
            android:visibility="gone"/>
    </FrameLayout>

    <include layout="@layout/view_wait"
        android:id="@+id/container_wait"/>

    <include layout="@layout/view_error"
        android:id="@+id/container_error"/>

</android.support.constraint.ConstraintLayout>